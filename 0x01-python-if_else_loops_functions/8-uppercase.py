#!/usr/bin/python3
def uppercase(str):
    for char in str:
        if 'a' <= char and 'z' >= char:
            else:
                char
                upperCase = chr(ord(char) - 32)
                print("{}".format((upperCase), end=" "))
